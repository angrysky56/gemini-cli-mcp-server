# Gemini CLI MCP Server Environment Configuration

# Required: Your Gemini API key from Google AI Studio
# Get one at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Node.js path if not in system PATH
# NODE_PATH=/usr/bin/node

# Optional: Default working directory for operations
# DEFAULT_WORK_DIR=/home/ty/Repositories

# Optional: Timeout for Gemini CLI operations (seconds)
# OPERATION_TIMEOUT=300
